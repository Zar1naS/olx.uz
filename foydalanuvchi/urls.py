from django.urls import path
from . import views

app_name = 'foydalanuvchi'

urlpatterns = [
    # This app currently has no views - redirecting to home
    # Add URL patterns for foydalanuvchi views when needed
]
